var React = require('react');

/*
  just trying to use redux for the money lending app + react for templating
*/

function attachPage(page) {
  window.location.href = '/pages/'+page+'/page.html';
}
